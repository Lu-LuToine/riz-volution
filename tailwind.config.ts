export default {
    theme: {
        extend: {
            colors: {
                button: "hsl(var(--button))",
                "button-accent": "hsl(var(--button-accent))",
            },
        },
    },
}
